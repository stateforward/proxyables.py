[project]
name = "proxyables-python"
version = "0.1.0"
description = "Peer-to-peer remote object proxies over multiplexed streams"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "msgpack>=1.1.0",
]
